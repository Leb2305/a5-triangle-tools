let
func sum (a: Integer, b: Integer):Integer ~
a+b;
func product (a: Integer, b: Integer):Integer ~
a*b;
var i: Integer;
var o: Integer
in
  begin
  getint (var i);
  getint (var o);
  puteol();
  putint(i);
  puteol();
  putint(o);
  puteol();
  if i=o then
   begin
   putint(sum(i,o));
   puteol();
   putint(product(i,o));
   puteol();
   put('s');
   put('a');
   put('m');
   put('e');
   end
  else
  begin
  putint(product(i,o));
  puteol();
  putint(sum(i,o));
  end;
end